<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vegt.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.vegt.entity.user">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="salt"     jdbcType="VARCHAR" property="salt"/>
        <result column="phone" jdbcType="VARCHAR" property="phone" />
    </resultMap>

    <select id="Sel"   parameterType="String" resultMap="BaseResultMap">
        select * from users where username = #{username} and password = #{password}
    </select>

    <select id="getuser"   parameterType="String" resultMap="BaseResultMap">
        select id,username,password,phone,salt from users where username = #{username}
    </select>

    <insert id="Adduser" useGeneratedKeys="true" keyProperty="id">
        insert into users (username,password,phone,salt) values (#{username},#{password},#{phone},#{salt})
    </insert>

</mapper>